import 'package:flutter/material.dart';

class RahebScreen extends StatelessWidget {
  const RahebScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Raheb Screen"),
    );
  }
}
